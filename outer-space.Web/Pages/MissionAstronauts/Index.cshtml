@page
@model outer_space.Web.Pages.MissionAstronauts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MissionAstronaut[0].MissionAstronautRole)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MissionAstronaut[0].Mission)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MissionAstronaut[0].Astronaut)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MissionAstronaut) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MissionAstronautRole)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mission.MissionName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Astronaut.AstronautNameFull)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
